package com.pelni.boarding.ticket.repository;

import com.pelni.boarding.ticket.entity.LogNew;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LogNewRepository extends JpaRepository<LogNew, Integer> {
}